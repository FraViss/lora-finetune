# Install with: uv pip install -r requirements.txt
# PyTorch CPU builds are hosted on the PyTorch wheel index.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.12.1+cpu
transformers==5.12.1
datasets==5.0.0
numpy==2.5.0
matplotlib==3.11.0
jupyter==1.1.1
